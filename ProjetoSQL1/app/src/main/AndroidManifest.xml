<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projetosql">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Projeto Cadastro"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CadastrarUsuario"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ListagemCursos" />
        <activity android:name=".CadastroMatricula" />
        <activity android:name=".CadastroPeriodo" />
        <activity android:name=".CadastroCurso" />
        <activity android:name=".CadastroAlunoDisciplina" />
        <activity android:name=".CadastroDisciplina" />
        <activity android:name=".CadastroAluno" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>